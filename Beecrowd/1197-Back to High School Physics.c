#include<stdio.h>
#include<string.h>
#include<stdlib.h>
#include<math.h>

int main()

{
    int n,a,x,y,z,i,j,k;
    //char a[],b[],c[];
    while(scanf("%d %d",&x,&y) != EOF)
    {
        n=0;
        n=2*x*y;
        printf("%d\n",n);
    }
    return 0;
}
